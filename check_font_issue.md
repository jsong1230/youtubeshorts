# 폰트 깨짐 문제 해결 가이드

## 현재 상황
- ✅ AppleGothic.ttf: 존재하고 렌더링 성공
- ✅ Arial Bold.ttf: 존재하고 렌더링 성공
- ❌ NanumGothic 시리즈: 시스템에 없음

## 사용자가 도와줄 수 있는 것

### 1. 구체적인 정보 제공
다음 정보를 알려주시면 정확히 진단할 수 있습니다:

- **어떤 문자가 깨지나요?**
  - 한글 전체가 사각형(□)으로 표시되나요?
  - 특정 문자만 깨지나요? (예: "갓생"의 "갓"만 깨짐)
  - 영문도 깨지나요?

- **언제 깨지나요?**
  - Hook 텍스트만 깨지나요?
  - 자막도 깨지나요?
  - 특정 영상만 깨지나요?

- **로그 확인**
  영상 생성 시 다음 로그를 확인해주세요:
  ```
  ✅ 폰트 로드 성공: ...
  ⚠️ 폰트 렌더링 테스트 실패 ...
  ❌ 모든 한글 폰트 로드 실패 ...
  ```

### 2. 폰트 파일 직접 제공
시스템 폰트가 문제라면, 직접 폰트 파일을 제공해주실 수 있습니다:

1. **한글 폰트 파일** (.ttf 형식)
   - 예: NanumGothicBold.ttf, NotoSansKR-Bold.ttf 등
   - 위치: 프로젝트 루트의 `fonts/` 폴더에 저장

2. **영문 폰트 파일** (.ttf 형식)
   - 예: Arial-Bold.ttf, Roboto-Bold.ttf 등

### 3. 폰트 경로 직접 지정
특정 폰트 경로를 알고 계시다면 알려주세요:
- 예: `/Users/사용자명/Library/Fonts/원하는폰트.ttf`

## 다음 단계
위 정보를 제공해주시면:
1. 폰트 로드 로직 개선
2. 커스텀 폰트 경로 지원 추가
3. 폰트 렌더링 테스트 강화

또는 다음 명령어로 더 자세한 정보를 확인할 수 있습니다:
```bash
# 최근 생성된 영상의 로그 확인
tail -100 logs/*.log | grep -i font

# 실제 사용된 폰트 확인
python3 -c "from src.generators.video.video_editor import VideoEditor; ..."
```
