# YouTube API
google-api-python-client==2.108.0
google-auth-httplib2==0.1.1
google-auth-oauthlib==1.1.0

# 영상 처리
moviepy==1.0.3
Pillow==10.1.0
opencv-python==4.8.1.78

# AI 및 텍스트 처리
openai>=2.0.0
anthropic>=0.34.0
httpx>=0.24.0
requests==2.31.0
replicate>=0.25.0  # Replicate API (고품질 TTS 지원)

# 음성 생성 (TTS)
gTTS==2.5.1
pydub==0.25.1
google-cloud-texttospeech>=2.16.0  # Google Cloud TTS (한글 발음 우수, 선택사항)

# 스케줄링
schedule==1.2.0
python-dotenv==1.0.0

# 유틸리티
python-dateutil==2.8.2
pytz==2023.3

# 웹 대시보드
flask>=3.0.0

# 테스팅
pytest>=7.4.0
pytest-mock>=3.12.0

# 트렌드 수집
praw>=7.7.1  # Reddit API (선택사항, RSS 피드 사용 시 불필요)
pytrends>=4.9.0  # Google Trends API
# requests는 이미 requirements.txt에 포함되어 있음 (RSS 피드 파싱용)
pydantic-settings>=2.0.0
mypy>=1.0.0
types-requests>=2.31.0
types-python-dateutil>=2.8.0
types-pytz>=2023.3,<2024
ruff>=0.1.0
black>=23.0.0
